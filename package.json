{
  "name": "project",
  "version": "1.0.0",
  "description": "GYM",
  "main": "server.js",
  "homepage": "https://gym-platform.onrender.com/",
  "scripts": {
    "build": "  npm run build --prefix client   ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd backend && npm run back",
    "dev":"cd backend && npm run back"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
