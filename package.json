{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install && npm install --prefix client && npm run build --prefix client &&  npm install --prefix backend  ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"concurrently \"cd client && npm start\" \"cd backend && npm run back\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
